{
  "name": "generic-reverse-proxy",
  "version": "1.0.0",
  "description": "A simple generic reverse proxy for any given url",
  "scripts": {
    "start": "node index.js -r generic_proxy.js -H 0.0.0.0 -p ${PORT:-8888}"
  },
  "engines": {
    "node": "10.x"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "micro-proxy": "^1.1.0"
  }
}
